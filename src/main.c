#include "cpu.h"

int main(int argc, char *argv[]) {
    /*
     * calls the function and pass the parameters
     */
    run_program("/home/gustanol/projects/cpu-simulator/tests/program.bin", "rb");
    return 0;
}
